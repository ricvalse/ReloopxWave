/**
 * AUTO-GENERATED — do not edit by hand.
 *
 * Regenerate with `./scripts/generate-api-types.sh` (runs openapi-typescript
 * against the FastAPI backend). CI fails if this file drifts from the live
 * OpenAPI schema (section 3 of the architecture doc).
 *
 * This placeholder compiles against the empty schema shape so the workspace
 * installs before the backend is running. The first real generation will
 * overwrite it.
 */
export interface paths {}
export interface components {
  schemas: Record<string, never>;
}
export type operations = Record<string, never>;
